@model MvcConcepts.Models.ProjectAssignViewModel
@{
    ViewBag.Title = "AssignUsers";
}
<div id="wrapper">
    <!-- Navigation -->
    <!-- Page Content -->
    <div id="page-wrapper">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">
                    <div class="row">
                        <div class="col-md-8">
                            @using (Html.BeginForm())
                            {
                                @Html.HiddenFor(p => p.Id)
                                @Html.ListBoxFor(p => p.SelectedUsers, Model.UserList)
                                <input type="submit" value="Submit!" />
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    </div>
